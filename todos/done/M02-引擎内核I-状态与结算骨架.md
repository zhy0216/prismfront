---
title: M2 · 引擎内核 I：状态与结算骨架
date: 2026-08-07
tags: prismfront, 里程碑, M2, 引擎, 状态模型, RNG, 结算栈, 确定性
milestone: M2
status: done
estimate: 3–4d
depends_on: [M1]
blocks: [M3]
risk: normal
---

# M2 · 引擎内核 I：状态与结算骨架

> 索引见 [README](../README.md)。原文见《[实现步骤与里程碑](../../docs/Prismfront%20实现步骤与里程碑.md)》§3。

**产出**：状态 / RNG / 结算栈 / 事件流 ｜ **估算** 3–4d ｜ **依赖** M1 ｜ **解锁** M3

---

## 任务

- [x] 状态模型：扁平 `entities` 表、`zones`、`slots: [(EntityId|null)[], (EntityId|null)[]]`、
      `PlayerData`（`crystals` / `crystalCap`）、`stack`、`pendingInput`。
      **纯数据，实体用 id 互相引用**（框架 §3.1）——这条从第一行代码就要守死。
      → `state/`（`game-state.ts` 是字段权威）
- [x] `RngState` + `nextInt`，种子入状态。→ `rng/`（xoroshiro + 拒绝采样，`state.rng`）
- [x] `resolve()` 六步流水线（框架 §4.1）：绑定上下文 → 拦截器 → handler → 触发入栈 →
      死亡结算 → 光环重算。触发/拦截/光环先留空实现，管线先立起来。
      → `resolve/resolve.ts`。②④⑥ 是**语义正确的退化实现**（无源 ⇒ 空链 / 空 Σ）
      而非抛异常的占位符，M5 填真语义；⑤ 死亡结算是真实现（含不动点）。
- [x] 事件日志与 `GameEvent`。→ `events/`（25 个 EventName，`drainEventLog` 不变量）
- [x] 不碰 DSL：手写几个临时 handler，跑通"抽牌 → 放单位到格 → 手动 strike → 死亡"。
      → `handlers/`（`M2_HANDLERS`）+ `__tests__/walkthrough.test.ts`
- [x] **把框架 §4.1 的四条时序规则抄成 `resolve.ts` 顶部注释**（文档明确要求）。
      → 逐字抄录，并标注每条规则**落在哪个文件**（改语义去那里，不要改六步顺序）

---

## 完成标志

```bash
bun test packages/engine/src/__tests__/determinism.test.ts
```

- [x] 两条确定性测试（架构文档 §6.1）通过

**实测**（2026-08-08）：11 passed。engine 全量 150 passed / 0 failed，
行覆盖 99.47%；`turbo lint typecheck boundaries` 全绿。

## 顺带处置

- [x] **R-M0-2 覆盖率闸门**（[风险登记册](../风险登记册.md)）。原条目只说「bunfig 不被
      子目录继承」，实测还有两个更深的坑：架构 §7 逐字给的行内表写法
      `coverageThreshold = { line = 0.8 }` 会被 bun 1.3.14 **静默忽略**（写 1.0 也 exit 0），
      且标量阈值是**逐文件**判定而非全局。三份 bunfig 已改标量并各就各位，
      engine / ir 两包的闸门现在真的会拦。详见风险登记册的处置小节。
- [x] **engine 补 `"test": "bun test"` 脚本**。此前 `bunx turbo test` 只跑 ir，
      engine 的测试**一条都没进 CI**（CI 注释里原就写着「待加回：engine（M2）」）。
      现在 `turbo test` 跑 521 条。

---

## 注意

第二条（序列化往返）是**架构腐化的探针**，它一红就说明状态里混进了函数或 class 实例。
