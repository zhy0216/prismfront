---
title: 风险登记册
date: 2026-08-07
tags: prismfront, 风险, 里程碑, 工程管理
---

# 风险登记册

> 索引见 [README](./README.md)。原文见《[实现步骤与里程碑](../docs/Prismfront%20实现步骤与里程碑.md)》§5。
> 风险触发或消除时就地更新状态列。

| 风险 | 触发点 | 影响 | 应对 | 状态 |
|---|---|---|---|---|
| Colyseus 服务端与浏览器 SDK schema 裂口 | [M0](./done/M00-工程骨架与选型验证.md) | 联机层选型推翻 | 首选不用 Schema；`transport/` 隔离层让影响限于一个目录 | ✅ **已消除**（M0/S1） |
| `@colyseus/bun-websockets` 较新、未经大规模验证 | [M0](./done/M00-工程骨架与选型验证.md) / [M9](./M09-Colyseus服务端.md) | 传输不稳 | 退回 `@colyseus/ws-transport`（Bun 的 Node 兼容足以跑 `ws`），业务代码不动 | ✅ **已验证可用**（M0/S1），退路未启用 |
| **turbo boundaries 的 tag deny 对外部 npm 包完全无效** | [M0](./done/M00-工程骨架与选型验证.md)/S3 起长期 | 禁令 2 与 §6.3 无机器强制 | 见下方专条 | ⚠️ **已触发，未处置** |
| **Vite 8 在 Bun 下改 `vite.config.ts` 会重启挂死** | [M10](./M10-Phaser客户端.md) | dev 体验 | Ctrl-C 重跑；或该场景退回 Node 跑 Vite | ⚠️ 已知，毛刺级 |
| **`bunfig.toml` 不被子目录继承 → §7 覆盖率闸门失效** | [M2](./done/M02-引擎内核I-状态与结算骨架.md) | 覆盖率门槛形同虚设 | 各包各放一份 `bunfig.toml` + 阈值改标量；见下方专条 | ✅ **已消除**（M2） |
| 同时结算的两个"不"被实现错 | [M3](./M03-引擎内核II-回合与同时结算.md) | 战斗手感全变，且随机对局未必显形 | 四条独立测试，不靠 fuzz 兜 | 未触发 |
| 亡语递归无限循环 | [M5](./M05-触发拦截光环.md) | 线上房间卡死 | 深度上限 + 专门测试（框架 §13 坑 5） | 未触发 |
| `Repeat` vs `.random(n)` 求值时机混淆 | [M4](./M04-DSL求值器与首批卡.md) 起长期 | 卡牌行为静默错误 | 进 review checklist；两条对照测试 | 未触发 |
| fuzz 暴露深层时序 bug | [M8](./M08-bot-CLI-fuzz-golden-replay.md) | 可能回头改 M3/M5 | M8 预留 50% 缓冲；这正是 fuzz 的价值，不是意外 | 未触发 |
| 客户端动画期间的输入 | [M10d](./M10-Phaser客户端.md) | 操作丢失或错位 | 本地排队 + 服务端按 `seq` 校验，别让动画阻塞输入（框架 §13 坑 6） | 未触发 |
| 9 格棋盘上 AoE 过强 | [M12](./M12-平衡闭环.md) | 数值失衡 | 《数值基准》§9 已标记为"最先观察的一行"，改 §4 价格表一行即可 | 未触发 |
| **属性减益不受色轮约束 → 绿色获得变相解牌** | [M11](./M11-PF1全卡池与lint.md) 写卡 / [M12](./M12-平衡闭环.md) 验证 | 绿色越权：它本该只有决斗这一种解牌手段 | [决策 #10c](./决策待办.md) 拍板放行（§1.2 是黑名单，不为没有的卡先猜规则）。M11 靠写卡自律，M12 靠胜率兜底；若绿因此过强，给 §1.2 补「属性减益」行（红副/蓝主/绿禁）并为减益定价 | 未触发（**已知设计缺口**） |
| **`playerActions` 若将来被开放，§1.2 方向操作行同时作废** | [M3](./M03-引擎内核II-回合与同时结算.md) 之后任何时候 | 红失去主色身份、绿的方向禁令失效，《数值基准》§1.2 要重写 | [决策 #3](./决策待办.md) 拍板恒关，且引擎在**配置校验期对 `move_unit`/`set_direction` 直接抛错**，不留静默无效开关 | 未触发（已用抛错固化） |
| Phaser 4 新版本踩坑 | [M10](./M10-Phaser客户端.md) | 客户端进度 | 编排层与显示层分离，回退 3.90 只动最上层 | 未触发 |
| 单人开发 M10 拖长 | [M10](./M10-Phaser客户端.md) | 总工期 | M10a–c 靠 replay 与引擎解耦，可随时暂停去推 M11 卡池 | 未触发 |

---

## M0 spike 实测新增的三条（2026-08-07）

### ⚠️ R-M0-1 · `turbo boundaries` 的 tag deny 对外部 npm 包完全无效

**实测**（M0/S3，含隔离对照仓）：`turbo.json` 里 `boundaries.tags.pure.dependencies.deny`
的六项 `colyseus` / `colyseus.js` / `phaser` / `vite` / `zod` / `@types/bun`
**全是死配置**。turbo 2.10.8 的依赖图里只有 workspace 包节点，deny 列表里凡不是 workspace
包名的字符串匹配不到任何节点，就被静默当成 no-op —— **turbo 不会警告这条 deny 没匹配上任何东西**。

判别实验：把 `phaser` 真加进 `packages/engine` 的 `dependencies` 并 `bun install`，
`import "phaser"` 原样保留、`pure` tag 原样保留 → `turbo boundaries` **绿，exit 0**。
同一份 `turbo.json` 里换成 workspace 包名（`@prismfront/engine`）立刻报
``Package `@prismfront/engine` found with tag listed in denylist``。

**好消息**：`no-rules.dependencies.deny` 的两项都是 workspace 包名，
所以 `apps/client` 那道围栏是**真的**，而且是 manifest 级（写进 package.json 就红，比 import 级更严）。

**当前实际守卫与它们的缺口**：

| 层 | 机制 | 有效范围 | 缺口 |
|---|---|---|---|
| import 语句 | `packages/*/biome.json` 的 `noRestrictedImports` | **只拦裸包名** | `import "zod/mini"` / `import "phaser/dist/phaser.js"` 全绿 |
| import 语句 | boundaries 的「未声明依赖」检查 | 能把 subpath 归约到包名 | **只在该依赖没进 `dependencies` 时成立** |
| manifest | 无 | — | **完全没有** |

**已验证的洞**：往 `packages/engine/package.json` 加 `"colyseus": "catalog:server"`，
`bunx turbo typecheck lint` + `bunx turbo boundaries` **三道闸全绿、零告警**。
§6.3「engine 的 `dependencies` 永远为空」这条铁律目前**没有任何机器强制**。

**处置**：本轮决定**只登记不改代码**（不动 §3.1 逐字块、不加 CI 脚本）。
建议的补法（留给 M1/M2 决定是否做）：加一条 CI 断言 —— `packages/*/package.json` 的
`dependencies` 只允许 `workspace:*`。它能**同时堵上 manifest 洞与 subpath 洞**
（依赖不进 manifest，subpath import 就会被 boundaries 的未声明依赖检查拦下）。

### ✅ R-M0-2 · `bunfig.toml` 不被子目录继承，§7 的覆盖率闸门曾是纸面条款（M2 已处置）

**实测**：bun 1.3.14 只从 **cwd** 读 `bunfig.toml`（外加 `~/.bunfig.toml`），不往上层找。
而 `turbo test` 跑各包的 `test` 脚本时 cwd 是**包目录** —— 所以根 `bunfig.toml` 的
`preload` / `coverage` / `coverageThreshold = { line = 0.8 }` 在 `bunx turbo test` 下
**一律不生效**（沙盒复现：cwd=根时 preload 执行且打覆盖率表，cwd=子目录时两者都没有）。

架构文档 §7 给了 `bunfig.toml` 全文，本轮按「逐字采用」原样保留，没有改。

**M2 落测试时必须三选一**：各包各放一份 `bunfig.toml`；或包级 `test` 脚本显式带
`--preload` / `--coverage`；或改成从仓库根跑一次 `bun test`。

#### M2 的处置（2026-08-08）

**选了第一条**：`packages/engine/bunfig.toml` + `packages/ir/bunfig.toml` 各一份，
CI 仍然只跑一条 `bunx turbo test`，turbo 的按包缓存与并行都保住。
代价是新包落第一条测试时要连 `bunfig.toml` 一起加（已写进 `.github/workflows/ci.yml`
的 test 脚本约定一节）。

**第二条被实测排除**：`bun test --help` 里**没有**覆盖率阈值的命令行开关 ——
`--coverage` 只产报表，`coverageThreshold` 是 bunfig-only。走这条只有报表没有闸门。

**顺带查出两个更深的坑（原条目没有覆盖，光换文件位置并不能修好）**：

1. **行内表写法会被静默忽略。** `coverageThreshold = { line = 0.8 }` ——
   也就是架构 §7 逐字给出的那一行 —— 在 bun 1.3.14 下**完全不生效**：
   写成 `{ line = 1.0 }` 依然 exit 0。只有标量 `coverageThreshold = 0.8` 才真的拦。
   即使当初把根 bunfig 原样复制到各包，闸门**照样是纸面条款**。
   三份 bunfig 已全部改为标量写法。

   | 写法 | 阈值 1.0 时的退出码 |
   |---|---|
   | `coverageThreshold = 1.0` | `1` ✅ 生效 |
   | `coverageThreshold = { line = 1.0 }` | `0` ❌ 静默忽略 |
   | `coverageThreshold = { line = 1.0, function = 1.0, statement = 1.0 }` | `0` ❌ 静默忽略 |

2. **标量阈值是逐文件判定，不是全局判定。** 实测边界卡在 0.73/0.74，恰好等于当时
   **最低的那个文件**（`state/queries.ts` 73.33% funcs），而彼时全局是 98.6%。
   这比预期更严（也更有用）：新增一个没被测到的导出函数会直接让 CI 红。
   但**不能按全局覆盖率估自己达不达标**。
   为过闸，M2 补了 6 条针对 `state/queries.ts` 与 `state/entity.ts` 未覆盖导出的测试
   （悬空 id、base 与生效标志位之分、叠加原语的纯函数性等），engine 现为 99.47% 行覆盖。

**闸门不达标时 bun 不打印任何提示**，只有退出码 —— 排查时别在日志里找报错信息。

架构 §7 的 `bunfig.toml` 全文仍是坏写法，按本仓惯例（同 R-M0-3）**不改文档、只登记偏离**。

### ⚠️ R-M0-3 · Phaser 4 的 ESM 产物没有 default 导出，但类型说有

**实测**：`phaser@4.0.0` 的 `exports.import` 指向 `dist/phaser.esm.js`（webpack 单文件包），
**只有具名导出**；而 `types/phaser.d.ts` 结尾是 `export = Phaser`。
我们 `tsconfig` 的 `module: "preserve"` 隐含 `esModuleInterop`，于是
`import Phaser from "phaser"` **能干净地过 `tsc`**，一进浏览器就炸：

```
Uncaught SyntaxError: The requested module '/node_modules/.vite/deps/phaser.js'
does not provide an export named 'default'
```

**类型检查完全拦不住这条。** [M10](./M10-Phaser客户端.md) 的硬规矩：
全仓统一 `import { Game, Scene, AUTO } from "phaser"`，**禁 default 导入**。

---

## 三个 ★ 高风险节点

[M3](./M03-引擎内核II-回合与同时结算.md)（战斗同时结算）、
[M5](./M05-触发拦截光环.md)（触发/拦截时序）、
[M8](./M08-bot-CLI-fuzz-golden-replay.md)（fuzz 暴露深层 bug）——
这三处各留出 **50% 的缓冲**。
