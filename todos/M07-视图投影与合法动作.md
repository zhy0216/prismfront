---
title: M7 · 视图投影与合法动作
date: 2026-08-07
tags: prismfront, 里程碑, M7, 隐藏信息, 投影, legalActions
milestone: M7
status: todo
estimate: 2–3d
depends_on: [M6]
blocks: [M8]
risk: normal
---

# M7 · 视图投影与合法动作

> 索引见 [README](./README.md)。原文见《[实现步骤与里程碑](../docs/Prismfront%20实现步骤与里程碑.md)》§3。

**产出**：`project` / `legalActions` ｜ **估算** 2–3d ｜ **依赖** M6 ｜ **解锁** M8

---

## 任务

- [ ] `project(state, viewer)` / `projectEvent(state, ev, viewer)`，按框架 §6 的可见性表。
- [ ] **隐藏牌必须保留稳定 `entityId`**，只把 `cardId` 置 `null`——否则
      "抽牌→飞入手牌→打出翻开"的动画在客户端接不上。
- [ ] `legalActions(state, player)` + `IllegalReason`，**带上色门缺色信息**
      （客户端要显示"没有红色光源"，不是错误码）。
- [ ] `legal` 只是给 UI 置灰用的方便字段，**不是权威**——服务端收到意图必须重新完整校验
      （M9 落实）。
- [ ] `legalActions` **只实现 `play_card` / `pass` 两个分支**，不为 `move_unit` /
      `set_direction` 写死代码（[决策 #3](./决策待办.md) 已拍板恒关，M3 在配置校验期抛错）。
      多写这两个分支不只是 YAGNI——它们会把动作空间从「手牌张数」炸成
      「手牌 × 9 格 × 方向」，直接抬高 M8 的 fuzz 分支因子与 M10 的交互复杂度。

---

## 完成标志

```bash
bun test packages/engine/src/__tests__/hidden-info.test.ts
```

- [ ] 把发给玩家 B 的所有字节 grep 一遍，断言不含玩家 A 手牌的任何 cardId

---

## 注意

**这一步不要往后挪**（框架 §13 坑 4）。隐藏信息不能事后补。
