---
title: M0 · 工程骨架与选型验证
date: 2026-08-07
tags: prismfront, 里程碑, M0, 工程骨架, turborepo, spike
milestone: M0
status: todo
estimate: 1–1.5d
depends_on: []
blocks: [M1]
risk: normal
---

# M0 · 工程骨架与选型验证

> 索引见 [README](./README.md)。原文见《[实现步骤与里程碑](../docs/Prismfront%20实现步骤与里程碑.md)》§3。

**产出**：8 包骨架、turbo、CI、**技术 spike** ｜ **估算** 1–1.5d ｜ **依赖** 无 ｜ **解锁** M1

---

## 任务

- [ ] `bun init` → 根 `package.json` 的 `workspaces` + `catalog`（架构文档 §1.1 给了全文）。
- [ ] 建 8 个空包，各自 `package.json` + `tsconfig.json` + 包级 `turbo.json`（打 tag）。
- [ ] `turbo.json`、`tsconfig.base.json`、`biome.json`、`bunfig.toml`。
- [ ] CI（GitHub Actions + `oven-sh/setup-bun@v2`），流水线按架构文档 §3.3。

### 三个 spike —— 这是 M0 的真实价值所在

- [ ] **Colyseus × Bun**：`colyseus@0.17.10` + `@colyseus/bun-websockets@0.17.13`
      起一个空房间；用 `colyseus.js@0.16.22` 连上，双向发一条消息，断线重连一次。
      → 结论回填架构文档 §1.2 风险 A / B，决定是否使用 Schema（[决策 #1](./决策待办.md)）。
- [ ] **Vite 8 × Bun × Phaser 4**：`bun run vite` 起 dev server，渲染一个空场景 + 一个 Sprite。
- [ ] **boundaries 生效**：故意在 `packages/engine` 里 `import "phaser"`，
      确认 `bunx turbo boundaries` 变红，然后删掉。

---

## 完成标志

```bash
bunx turbo typecheck lint boundaries   # 全绿
bun run spike:colyseus                 # 打印「joined / echo / reconnected」
bun run spike:client                   # 浏览器出现一个方块
```

- [ ] 三条命令全部通过

---

## 注意

**为什么 spike 必须在第一天**：架构文档 §1.2 的风险 A 是唯一可能推翻联机层选型的问题。
它花半天验证，或者花两周后返工。

## 相关风险

- Colyseus 服务端与浏览器 SDK schema 裂口 → 首选不用 Schema；`transport/` 隔离层让影响限于一个目录。
- `@colyseus/bun-websockets` 较新、未经大规模验证 → 退回 `@colyseus/ws-transport`，业务代码不动。

详见 [风险登记册](./风险登记册.md)。
