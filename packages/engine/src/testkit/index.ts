// @prismfront/engine/testkit —— 引擎测试夹具（架构 §4.1 的 exports 已点名 "./testkit"）
// 将来提供：建局 / 摆盘 / 推进到指定阶段的辅助函数，供 packages/cards 的单卡测试
//           与 packages/engine/src/__tests__/{timing,combat}/ 复用。
// 与 engine 主入口同样受 §2.2 禁令 5 约束：不得使用 Bun.* 或 bun: 模块（bun:test 例外，见 biome.json）。
// M0/T2 骨架占位。
export {};
